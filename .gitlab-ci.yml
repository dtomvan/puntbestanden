stages:
  - check

nix-flake-check:
  stage: check
  image: docker.nix-community.org/nixpkgs/nix-flakes
  script:
    - nix flake check -L
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
